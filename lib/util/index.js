'use strict';
/**
 * index module
 * @module index
 * @see module:index
 */
const containsSameItems = require('./contains-same-items');
const guessItemsTypes = require('./guess-items-type');

module.exports = {
  containsSameItems,
  guessItemsTypes,
};
