'use strict';
/**
 * index module
 * @module index
 * @see module:index
 */
const containsSameItems = require('./contains-same-items');
const guessItemsTypes = require('./guess-items-types');

module.exports = {
  containsSameItems,
  guessItemsTypes,
};
